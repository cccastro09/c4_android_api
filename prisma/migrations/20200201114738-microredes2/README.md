# Migration `20200201114738-microredes2`

This migration has been generated by CastroC4 at 2/1/2020, 11:47:38 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."MicroRedes2" (
    "color" text  NOT NULL DEFAULT '',
    "familia" text  NOT NULL DEFAULT '',
    "foto" text  NOT NULL DEFAULT '',
    "id" SERIAL,
    "informacion_de_la_especie" text  NOT NULL DEFAULT '',
    "n_de_paredes_de_la_espora" text  NOT NULL DEFAULT '',
    "pais" text  NOT NULL DEFAULT '',
    "tamanio_um" text  NOT NULL DEFAULT '',
    "textura_de_la_espora" text  NOT NULL DEFAULT '',
    "tombre_cientifico" text  NOT NULL DEFAULT '',
    PRIMARY KEY ("id")
) 
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200201092649-microredes..20200201114738-microredes2
--- datamodel.dml
+++ datamodel.dml
@@ -6,14 +6,14 @@
 // The `url` field must contain the connection string to your DB.
 // Learn more about connection strings for your DB: https://pris.ly/d/connection-strings
 datasource pg {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 // Other examples for connection strings are:
-// SQLite: url = "***"
-// MySQL:  url = "***"
+// SQLite: url = "sqlite:./dev.db"
+// MySQL:  url = "mysql://johndoe:johndoe@localhost:3306/mydb"
 // You can also use environment variables to specify the connection string: https://pris.ly/d/prisma-schema#using-environment-variables
 // By adding the `generator` block, you specify that you want to generate Prisma's DB client.
 // The client is generated by runnning the `prisma generate` command and will be located in `node_modules/@prisma` and can be imported in your code as:
@@ -29,8 +29,21 @@
   name     String
   password String
 }
+model MicroRedes2 {
+  id                        Int    @id @default(autoincrement())
+  color                     String
+  foto                      String
+  familia                   String
+  n_de_paredes_de_la_espora String
+  pais                      String
+  tamanio_um                String
+  textura_de_la_espora      String
+  tombre_cientifico         String
+  informacion_de_la_especie String
+}
+
 model Microredes {
   id                        Int    @id @default(autoincrement())
   familia                   String
   nombre_cientifico         String
@@ -55,5 +68,5 @@
 // Next steps:
 // 1. Add your DB connection string as the `url` of the `datasource` block
 // 2. Run `prisma2 introspect` to get your data model into the schema (this will override this file and delete all comments!)
 // 3. Run `prisma2 generate` to generate Prisma Client JS
-// 4. Start using Prisma Client JS in your applicati
+// 4. Start using Prisma Client JS in your applic
```


